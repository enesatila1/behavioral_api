version: '3.8'

services:
  api:
    build: .
    container_name: behavioral_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
